<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend.mapper.CategoryMapper">
    <resultMap id="categoryMap" type="com.example.backend.entity.Category">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="sortOrder" column="sort_order" />
        <result property="createdAt" column="created_at" />
    </resultMap>

    <select id="findById" parameterType="Long" resultMap="categoryMap">
        SELECT * FROM category WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="categoryMap">
        SELECT * FROM category ORDER BY sort_order ASC, id ASC
    </select>

    <select id="findAllOrdered" resultMap="categoryMap">
        SELECT * FROM category ORDER BY sort_order ASC, id ASC
    </select>

    <insert id="insert" parameterType="com.example.backend.entity.Category" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, sort_order)
        VALUES (#{name}, #{sortOrder})
    </insert>

    <update id="update" parameterType="com.example.backend.entity.Category">
        UPDATE category
        SET name = #{name},
            sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="Long">
        DELETE FROM category WHERE id = #{id}
    </delete>
</mapper>
